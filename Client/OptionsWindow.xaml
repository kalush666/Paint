<Window x:Class="Client.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options"
        Width="250" Height="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        Topmost="True">
    <StackPanel Margin="20">
        <TextBlock Text="Select Color:" Margin="0,0,0,5"/>
        <ComboBox x:Name="ColorPicker" SelectedIndex="0">
            <ComboBoxItem Content="Black"/>
            <ComboBoxItem Content="Red"/>
            <ComboBoxItem Content="Green"/>
            <ComboBoxItem Content="Blue"/>
        </ComboBox>

        <TextBlock Text="Stroke Thickness:" Margin="10,10,0,5"/>
        <Slider x:Name="StrokeSlider" Minimum="1" Maximum="10" Value="2"
                TickFrequency="1" IsSnapToTickEnabled="True"/>

        <Button Content="Apply" Margin="0,15,0,0" Click="ApplyButton_Click"/>
    </StackPanel>
</Window>